@startuml listDeviceGroupsWithoutDevices
skinparam responseMessageBelowArrow true

title
  listDeviceGroupsWithoutDevices
end title

participant "requestor\n" as requestor
participant "listDeviceGroupsWithoutDevices\n" as indServ
database "RunningDS" as RunDS

requestor -> indServ
activate indServ

    note over indServ
      Retrieves [DeviceGroups]
    end note
    indServ -> RunDS
    activate RunDS
    indServ <-- RunDS
    deactivate RunDS

  loop for each DeviceGroup
    note over indServ
      If [device].length NOT > 0
    end note
    note right indServ
      THEN
    end note
    note right indServ
      Appends label of DeviceGroup
      to [deviceGroupList]
    end note
    note over indServ
      ELSE
    end note
  end loop

requestor <-- indServ: { [deviceGroupList] }
deactivate indServ

@enduml
