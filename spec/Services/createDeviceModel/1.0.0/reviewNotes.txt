(1)
wieso gibt es nur createDeviceModel, statt createOrUpdateDeviceModel?
wenn man die defaultFirmware oder defaultDeviceGroup ändern wollen würde, wäre das nicht möglich,
außer man löscht das deviceModel erst und legt es dann komplett neu an.

(2)
die defaultFirmware wird gar nicht gesetzt